<view class="menu-section">
  <menu-item
    wx:for="{{ arrMenus }}"
    wx:key="index"
    class="menu-item"
    item="{{ item }}"
    index="{{ index }}"
    bindtap="transfer(item)"
  ></menu-item>
</view>
