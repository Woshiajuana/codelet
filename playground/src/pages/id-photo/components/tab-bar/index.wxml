<scroll-view class="tab-bar" scroll-x enable-passive>
  <view class="tab-bar-content">
    <view
      wx:for="{{ 14 }}"
      wx:key="index"
      class="tab-bar-item {{ index === 0 ? 'is-active' : '' }}"
    >
      <text>全部-{{ index }}</text>
    </view>
  </view>
</scroll-view>
